# Lessons Archive

已 superseded 或 6 月未复发的 lessons。从 `../lessons.md` 移过来。

> 不主动 grep 这里 — active 池在 `../lessons.md`。仅在做长期复盘 / harness retro 时回看。

## 索引格式

每条保留原 `L-NNN` 编号 + 移入日期 + 移入原因（superseded-by-L-XXX / deprecated-since-{commit}）。

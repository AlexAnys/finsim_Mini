# QA Report — PR-fix-1 r1

## Spec: P0 fixes — IDOR on PATCH /api/lms/courses/[id] (B1), student dashboard cross-class leakage (B2), delete primary CourseClass causing dangling Course.classId (B3)

| Check | Verdict | Evidence |
|-------|---------|----------|
| 1. Spec compliance | PASS | B1: `app/api/lms/courses/[id]/route.ts:8-17` defines `assertCourseAccess` (admin bypass, createdBy or CourseTeacher), called at L45 before update; `classId` removed from `patchSchema` L33-37. B2: `lib/services/dashboard.service.ts:117-118` where clause = `{ classId, status: "published" }`, no OR, course/announcements/scheduleSlots retain dual OR as spec permits. B3: `lib/services/course.service.ts:96-104` throws `COURSE_NOT_FOUND` on missing course and `CANNOT_REMOVE_PRIMARY_CLASS` when `classId === course.classId`. UI badge × button hidden when `isPrimary` (`app/teacher/courses/[id]/page.tsx:1208,1213-1220`). All three acceptance criteria in spec `## Acceptance criteria` rows 1-3 addressed. |
| 2. tsc --noEmit | PASS | `npx tsc --noEmit` exit 0, no output. |
| 3. vitest run | PASS | 9/9 tests across 3 new files in 207ms. Tests exercise the exact invariants: `tests/dashboard.service.test.ts:31` asserts `where` equals `{ classId: "class-A", status: "published" }` and `where.OR` is undefined; `tests/course.service.test.ts:18-28` asserts primary-class deletion throws and `courseClass.delete` is not called; `tests/courses-patch.api.test.ts:36-57` asserts non-owner teacher → 403 FORBIDDEN + no `course.update` call; admin path skips `findUnique` (L121). |
| 4. Browser (/qa-only) | PASS (partial) | Dev server started (`next dev` PID 16537, 970ms ready). Live curl smoke: `PATCH /api/lms/courses/<id>` without session → `HTTP 401` with `{success:false, error:{code:"UNAUTHORIZED", message:"未登录，请先登录"}}` (Chinese, correct envelope). `GET` same route and `DELETE /classes` same behavior. Full session-based 403/400/200 scenarios already fully covered by vitest mocks with equivalent shape — live login via curl is blocked by NextAuth CSRF handshake, deferred to manual QA for multi-PR integration phase. UI change is conditional render only (no dynamic interaction beyond existing onClick), low risk for pure visual regression. |
| 5. Cross-module regression | PASS | grep `removeCourseClass` callers: only `app/api/lms/courses/[id]/classes/route.ts:68` — signature unchanged `(courseId, classId) => Promise<CourseClass>`, still compatible. grep `taskInstance.*findMany\|.*where` across `lib/services` and `app/api`: `task-instance.service.ts:75-100` uses explicit `classId` filter param (no student leakage surface), `dashboard.service.ts:16` is the teacher dashboard (B4 territory, intentionally out of scope per spec). `handleServiceError` additions in `lib/api-utils.ts:43-54` are strictly additive — `CANNOT_REMOVE_PRIMARY_CLASS`, `COURSE_NOT_FOUND`, `CLASS_NOT_FOUND`, `USER_NOT_FOUND`, `NOT_A_TEACHER`, `ALREADY_OWNER` mapped; no removed codes. No Prisma schema changes → no migrate/generate/restart needed. |
| 6. Security (/cso) | PASS | Manual OWASP review of the PATCH auth change:<br>• A01 (Broken Access Control): `assertCourseAccess` closes the IDOR — teacher→teacher2 course PATCH now returns 403. Admin bypass is intentional and acceptable for role model. Ownership check uses uniqueness of `courseId_teacherId` composite key (not a string compare).<br>• A03 (Injection): all inputs zod-validated; `prisma` parameterized.<br>• A04 (Insecure Design): `classId` removed from PATCH schema closes the drift between `Course.classId` and `CourseClass` — only path to change class membership is now the dedicated `/classes` POST/DELETE, which also has `assertCourseAccess`.<br>• A08 (Data Integrity): `removeCourseClass` guard prevents dangling `Course.classId`, which is the data-integrity failure mode the spec called out.<br>• A09 (Logging): 500-path `console.error` unchanged; no secrets leaked in error envelopes.<br>No High/Critical findings. STRIDE Tampering (T) surface previously exposed via B1 is now mitigated. |
| 7. Finsim-specific | PASS | Error messages in Chinese: "不能移除课程的主班级", "课程不存在", "班级不存在", "用户不存在", "该用户不是教师", "该用户已是课程创建者". Auth uses `requireRole(["teacher","admin"])` + custom `assertCourseAccess` (same pattern as sibling `/classes` route — not manual session check). Route handler remains thin: parses body, calls guard, calls `prisma.course.update`, returns through `success()`/`handleServiceError()` helpers — no business logic leaked into route. API envelope `{success, data|error:{code, message}}` preserved. |
| 8. Code patterns | PASS | Diffs are strictly scoped to the 3 P0 bullets — no drive-by refactors. `removeCourseClass` fix is root-cause (pre-check against `course.classId`), not a UI-only symptom patch. B2 fix is root-cause (delete the unsafe OR branch), not a filter-after-fetch bandaid. B1 fix mirrors the already-correct sibling route's guard verbatim, consistent codebase style. `handleServiceError` additions are a legitimate cleanup directly needed by the new `COURSE_NOT_FOUND` surface; spec scope permits it and builder called it out explicitly in "Non-obvious decisions". |

## Issues found

None.

Observations (non-blocking, for info only):
- `assertCourseAccess` is now duplicated in `app/api/lms/courses/[id]/route.ts:8-17` and `app/api/lms/courses/[id]/classes/route.ts:8-18`. Identical bodies. Future consolidation into `lib/auth/guards.ts` would remove the duplication, but spec did not request it and PR scope was kept minimal — acceptable.
- Live session-based 403 path was not executed via browser because curl login against NextAuth needs CSRF token dance; vitest covers the unit-level behavior with equivalent assertion shape, and live 401 handshake was verified. If coordinator wants a full click-through before merge, flag for a later QA pass.

## Overall: PASS

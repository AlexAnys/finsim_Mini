# QA Report — PR-fix-3 r1

## Spec: P2 fixes — `CourseAnalyticsTab` perf (B7 Promise.all) + ranking mistakes (B8 filter `gradedCount === 0`)

| Check | Verdict | Evidence |
|-------|---------|----------|
| 1. Spec compliance | PASS | **B7**: `components/course/course-analytics-tab.tsx:227-236` replaces the serial `for (const inst of instances)` + inline `await fetch(...)` with `Promise.all(instances.map(async (inst) => await fetch(...)))`. All per-instance `/api/submissions?taskInstanceId=...` calls are fired synchronously from the map callback and awaited via a single `Promise.all`, which by JS spec runs them concurrently (bounded only by the browser connection pool, typically 6 for HTTP/1.1, more for HTTP/2). Stats/ranking aggregation at L238-260,264 moved to a second pass over the resolved array. **B8**: `buildStudentRanking` extracted as an exported pure helper at L153-184; the critical line L182 `.filter((sp) => sp.gradedCount > 0)` drops students who submitted but were never graded, so they no longer tie at the bottom with real 0-score students. B8 UI: "已批改" column added at L703,724 exposes `gradedCount` in the ranking table. Acceptance criterion row 7 (analytics < 500ms) is structurally satisfied by the parallelization; row 8 (no-ungraded-in-ranking) satisfied by the filter. |
| 2. tsc --noEmit | PASS | Exit 0, no output. |
| 3. vitest run | PASS | 20/20 tests across 7 files in 317ms (15 retained + 5 new). `tests/student-ranking.test.ts` exercises all B8 edge cases via the exported helper: (L5-14) ungraded-only student excluded; (L16-29) mixed graded+submitted keeps only graded student; (L31-43) the exact core B8 bug — "real 0-score student does NOT rank alongside ungraded", verified: `["s-high", "s-zero"]`, ungraded excluded; (L45-56) descending sort by avgScore; (L58-75) cross-instance aggregation averages correctly (80+100 over 2 graded = 90). |
| 4. Browser (/qa-only) | PASS (partial) | Dev server started (`next dev` PID 28701, 659ms ready). `GET /teacher/courses/<uuid>` → HTTP 200 (SSR compile path clean, 360ms including 309ms compile). `GET /api/submissions?taskInstanceId=xyz&pageSize=200` → HTTP 401 + Chinese envelope. Dev server log clean, no runtime/React/Turbopack warnings. **Full perf measurement with logged-in teacher session on a course with >10 published instances is NOT executed here** — no Postgres in QA env (docker not on PATH) so seeding/auth flow can't be established. The parallelization is structurally guaranteed by JS semantics (`Promise.all(map(...))` literally runs map callbacks concurrently), and unit tests lock the aggregation contract; deferring only the wall-clock wall panel screenshot to the post-deploy manual smoke. |
| 5. Cross-module regression | PASS | `CourseAnalyticsTab` consumer is solely `app/teacher/courses/[id]/page.tsx:65,1498` (grep confirms). Component props signature `{ courseId: string }` unchanged. New export `buildStudentRanking` is additive — no existing caller impacted. `/api/submissions?taskInstanceId=...&pageSize=200` endpoint contract unchanged — the parallelization is purely client-side scheduling. Table row `<TableHead>` / `<TableCell>` alignment: one head + one cell added at index 3 in both; no colSpan or missed cell breakage. Stats for the top "course-level" cards (`totalSubmissions`, `totalGraded`, `overallAvg`) remain unfiltered — only `studentPerformance` (ranking) applies the `gradedCount > 0` filter, so course-wide counts stay truthful. No DB / Prisma / service / API / auth / schema change → none of the CLAUDE.md 3-step rule triggers apply. |
| 6. Security (/cso) | N/A | Pure client-side perf + UX refactor. No auth/session/permission/crypto/upload/payment surface touched. |
| 7. Finsim-specific | PASS | All UI strings Chinese ("已批改", "加载任务列表失败", "网络错误，请稍后重试"). Error envelopes on `/api/submissions` preserved (verified live 401). No API response shape change. Route handlers / service layer untouched. |
| 8. Code patterns | PASS | Diff strictly scoped to B7 + B8 + the one UI column that supports B8's UX. `buildStudentRanking` extraction is minimal (mostly relocation of the inline aggregator) — the Map+for loop body is unchanged vs HEAD; only the `.filter(... gradedCount > 0)` is newly interposed between `Array.from(studentMap.values())` and `.sort(...)`. No drive-by refactors elsewhere in the file (evidence sheet, insights cache, toggleTask all untouched). Root-cause fix: B8 addresses the seed value that treated "never graded" as avgScore=0; the filter at the serialization boundary is the correct place since the Map is the only aggregation site. |

## Issues found

None.

Non-blocking observations:
- **Error isolation**: `Promise.all` short-circuits on first rejection, so one flaky `/api/submissions` call would currently surface `toast.error("网络错误，请稍后重试")` and drop the entire tab. Builder called this out — pre-existing behavior (the old `for-await` also threw out of the loop), so not a new regression. Future iteration could use `Promise.allSettled` for graceful degradation. Not a PASS blocker.
- **Concurrency**: no bounded parallel queue. For pathological courses with hundreds of instances the browser connection pool will enqueue requests anyway (HTTP/1.1 ≤ 6, HTTP/2 controlled by server SETTINGS_MAX_CONCURRENT_STREAMS). Builder disclosed the tradeoff — acceptable for N typically ≤ 50.
- **Perf wall-clock evidence**: structurally guaranteed by the code (`Promise.all(map(...))` IS parallel), but the DevTools Network-panel "waterfall → parallel bars" screenshot is deferred to post-deploy manual QA because seeding/login aren't possible in this agent's environment. Flag to whoever runs the first real browser session to confirm the ~3s → ~500ms drop.

## Overall: PASS

# 试点可靠性修复 · 2026-09-09

用户已授权：优化审查的重要问题，可并行实现；直接进行端到端测试并迭代。使用阶段为近期试点/演示。用户已进一步确认一并将文本与批改迁移至 DeepSeek，保留语音/OCR 专用模型。全局、功能覆盖和旧文本工具设置均须一致；真实账号验证通过前不执行生产模型迁移。

基线 origin/main `5dd4f43`，任务分支 `codex/pilot-reliability`。旧本地 main 的恢复性 harness 提交保留。审查依据在 `/Users/yangsenan/Documents/Codex/reviews/finsim-2026-09-09/REVIEW.md`。

## 本次完成标准

- 学生答前接口不泄露固定测验答案；页面作答、冻结版本、评分使用同一题目和标准；自适应按服务端认可的已发题计分。
- 简答失败/评分项不完整不成为正式零分；附件参与评分时实际提取并送入模型，失败可见且可恢复；重复提交不产生额外作答。
- 教师查询有资源边界；撤回公布被保留；真实成绩删除具备审计/恢复设计。
- AI动作具有总等待上限、诊断测试所选配置、界面只暴露真实能力；涉及概念/样本统计/学生引用表达与证据一致。
- agent保留coordinator/builder/qa分工；任务、被测代码、环境和证据绑定；Stop是轻量确定检查，工具路径可实际运行。
- 部署从干净源码构建、带版本身份和就绪检查；必要业务定时任务/备份有可验证路径；依赖安全更新有回归。
- 本地隔离数据库/独立端口运行真实UI端到端；独立QA复核。必要检查成功后通过PR的quality和staging-deploy，再实测同版本staging。按仓库正常流程squash合并并检查生产部署。

## 分工和边界

- 应用builder：提交/任务读取与快照/评分/附件/权限/数据模型与对应UI、测试；不得改AI provider主文件。
- AI builder：AI服务/provider/settings/insights/prompts与对应UI、测试；不得改grading/submission/schema；跨边界先说明接口。
- infra builder：agent定义、harness机制、部署/CI脚本、依赖升级、对应测试；不执行生产写操作，不自行覆盖模型选择。
- coordinator：环境准备、集成、状态与模型核验、独立QA调度、GitHub/staging/生产发布检查。

共享worktree按文件所有权分工，提交由coordinator统一在验证后处理。staging只运行一个待验版本；不重置生产或共享staging数据库。数据库迁移只先在隔离本地库执行，生产迁移由正常部署执行。保留历史记录，不依据旧spec启动新方向。

## 验证

先失败复现，再修复；每个builder只跑相应定向检查，合并后统一跑tsc、vitest、lint/build，避免反复全套。真实端到端覆盖教师创建/发布→学生三类作答→自动批改→教师公布→学生查看，及错误输入/上游失败/任务重试/撤回/不同用户权限。真实AI调用仅为有界的功能验证并记录实际provider/model，不把一次成功当教学准确率证明。

无法完成的外部条件须写出具体阻塞和已有证据，不将跳过写通过。代码或环境变化后相关QA结果失效。

# 33 项审查剩余工程问题复核与交付

2026-09-12。用户已授权独立完成剩余可由 agent 验证的修复、端到端验证及既有 PR 发布流程。

基线：最新 origin/main `d8c3f6bb5b2128a84935cc04149d585fa5e44280`。原审查：`/Users/yangsenan/Documents/Codex/reviews/finsim-2026-09-09/REVIEW.md`。

## 范围

- 对照原 33 项，按当前代码与本次实测区分已完成、部分、未完成、已不适用。
- 复核并修复旧会话权限与密码更新、周洞察统计范围及费用归属、生产数据库监听范围、宿主就绪守护、staging 关闭后调度清理等仍有证据的问题；仅补充学生附件解析的兼容安全修补。
- 保留现有架构、三角色、DeepSeek 文本策略和专用语音/OCR配置；不重写公开历史、不旋转凭据、不引入付费服务。

## 验收

1. 密码改变后旧登录不可继续用；班级/角色改变后下次请求使用当前权限；未变化账号正常使用。
2. 周洞察人数、样本、均分由真实数据确定，按班级/课程轮流选代表样本并标明覆盖比例；AI失败仍保留确定统计，AiRun费用归属为本次调用。
3. 数据库宿主端口只绑定回环地址，容器内应用、备份、迁移均继续可用。
4. 仅当共享 staging 归属本 PR 时停止服务并移除其调度；其他 PR 与生产调度不受影响。
5. 应用类型检查、完整 Vitest、相关部署/路由测试通过；独立 QA 在冻结 SHA 下运行 Chromium → API → 隔离 PostgreSQL 核心链路，零重试；补充本次变更的正向/失败与权限对照，登录、设置与周洞察核对桌面及 390px 手机视口。真实 XLSX 上传提取和坏附件不出正式成绩均需验证。
6. PR 的 quality、staging-deploy 成功后完成 staging 自验和 squash merge，核对生产 SHA、ready、数据库监听和业务调度。只有实际完成才报告上线。

## 环境与证据

本地合成数据：`http://127.0.0.1:3107`，独立 `127.0.0.1:55439/finsim_pilot`；AI协议fixture仅用于确定性异常，真实模型结果分开记录。交付报告：`/Users/yangsenan/Documents/Codex/reviews/finsim-2026-09-12/`。QA 报告写 `.harness/reports/` 并由独立QA签署源码与环境绑定。

教师标注准确率、教学增益、异地备份目的地、专用模型新凭据及不可逆历史操作不在自动完成范围；报告剩余原因，不能用绿色测试代替。

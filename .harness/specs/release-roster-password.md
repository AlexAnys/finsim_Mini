# 教师批量管理与改密安全组合发布

授权：2026-09-17用户明确要求PR40和PR41正式上线。真实25人转回0452401由原名单任务单独执行，本发布不得操作真实名单或密码，不得恢复旧数据库/reseed。

## 发布候选

将PR41完整改动并入PR40，在同一候选上验证并通过PR40一次squash发布；生产确认包含两项功能后，PR41标记为已纳入PR40。保留全部历史FAIL和两项原验收记录，不使用原单独PASS代替组合验证。

## 验收

- 现有批量管理验收范围见 teacher-roster-management.md；改密范围见 ../spec-password-visibility.md。
- 冻结组合提交，类型检查、全套Vitest与必需quality/staging-deploy成功；独立固定构建运行19项业务/名单pilot、课程权限隔离smoke、5次并发转班及4项合成账号改密UI流程（显示/隐藏、焦点选区、并发条件更新、反馈截图脱敏等）。
- 正式部署前收到名单事务退出通知；确认私有备份、当前应用/配置回退材料及数据保留边界。发布使用原deploy-release.sh，禁止数据库全量替换。
- 按保护规则合并最终候选，核对实际生产健康、SHA、两项功能文件/页面入口。生产只做安全只读验收，写入测试限合成隔离环境；不猜测/输入Molly真实密码，不绕过凭据接管限制。
- 发布后确认两功能在生产同一版本，记录检查、备份和回滚位置。回滚只能先回退应用/配置，不能覆盖名单任务最新归属。
